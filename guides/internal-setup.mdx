---
title: "Internal Setup"
description: "Setup notes for the internal team"
---

This page is private. It has no `public` frontmatter and its navigation group has no `public` flag, so it inherits the site default (private when auth is configured).

## Environment access

Internal-only setup instructions live here.
